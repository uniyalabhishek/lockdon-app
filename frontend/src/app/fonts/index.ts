import localFont from 'next/font/local';

export const ccDeadlineDreaded = localFont({
  src: './CCDeadlineDreaded.ttf',
  variable: '--font-cc-deadline-dreaded',
});

export const ccDeadlineDreadedOpen = localFont({
  src: './CCDeadlineDreadedOpen.ttf',
  variable: '--font-cc-deadline-dreaded-open',
}); 